node_modules/
dist/
.env
*.db
*.db-journal
*.jsonl
__pycache__/
*.pyc
.DS_Store

# The raw Pulsoid export stays local. Its derived 10 s grid under data/real/
# IS committed on purpose -- docs/real/ is built from it.
pulsoid-report-*.csv

# Live capture lands in data/ as .jsonl/.db and is ignored by the patterns
# above. The synthetic CSVs in data/ are checked in on purpose -- they are the
# validation set, not runtime output.
