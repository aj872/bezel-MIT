node_modules/
dist/
.env
*.db
*.db-journal
*.jsonl
__pycache__/
*.pyc
.DS_Store

# Live capture stays local -- real recordings are not committed.
pulsoid-report-*.csv

# Live capture lands in data/ as .jsonl/.db and is ignored by the patterns
# above. The synthetic CSVs in data/ are checked in on purpose -- they are the
# validation set, not runtime output.
